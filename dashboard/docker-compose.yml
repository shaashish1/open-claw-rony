version: "3.9"

services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8002:8002"
    volumes:
      - dashboard_data:/app/data
      - ./.env:/app/.env:ro
    env_file:
      - .env
    restart: unless-stopped

volumes:
  dashboard_data:
